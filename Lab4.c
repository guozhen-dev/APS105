#include <stdio.h>

int main(int argc, char const * argv[]){
	int initAmount, payPM, purPM, yearIR, N;
	
	printf("Enter amount already owed on the card: ");
	scanf("%d",&initAmount);
	printf("Enter purchase amount per month: ");
	scanf("%d",&payPM);
	printf("Enter payment amount per month ");
	scanf("%d",&purPM);
	printf("Enter yearly interest rate as percent: ");
	scanf("%d",&yearIR);
	printf("Enter number of months to consider:");
	scanf("%d",&N);
	
}